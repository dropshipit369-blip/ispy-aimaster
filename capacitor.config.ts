import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.dropshipit369.ispyaimaster",
  appName: "iSpy AI Master",
  webDir: "dist",
  server: {
    androidScheme: "https",
  },
};

export default config;
